@AGENTS.md

# Notes for Claude

The canonical instructions live in [AGENTS.md](./AGENTS.md). This file
just re-exports them so Claude Code (and other tools that look for
`CLAUDE.md` specifically) pick them up automatically.

If you find yourself wanting to add Claude-only guidance here, ask
whether it actually belongs in `AGENTS.md` first — anything project-wide
should live there so every assistant benefits.
