/**
 * Placeholder for the Supabase-generated database types.
 *
 * Once the schema exists, regenerate this file with:
 *
 *   npx supabase gen types typescript \
 *     --project-id "$NEXT_PUBLIC_SUPABASE_PROJECT_ID" \
 *     --schema public > src/lib/supabase/types.ts
 *
 * Until then we use a permissive shape so the typed clients still compile.
 */

// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export type Database = {};
